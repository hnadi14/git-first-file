https://git-scm.com/
>in terminal or git bash
git status
git config --global user.name "hnadi14"
git config --global user.email "hnadi14@gmail.com"
git config --list
*******************
>set project to git:  go to project dir and  Then create .git dir and dont change dir and other file in this dir
> this project in git is repository under git
git init
************************
> untracked file in first run of git status
git status
git add filename.txt
git commit -m "message ..."
************************
> all files 
git add .
git log
git log --oneline
git log --stat
>**** q for exit
*****************************
> branch in git
git branch
git branch --list
***********************
> switch to branch
git checkout brnachName
> change branch name
git branch -m oldname new name
> merage this branch with branchname1
git merge branchname1
> for delete branch if branch not merge to master
git branch -D branchname
*****************
> back to point
git checkout HASHCODE
************
> DANGER  delete of effects of commit in git, just last commit
git revert HASHCODE
***************
> conflict when a commit effect same file
> just use revert for last commit

